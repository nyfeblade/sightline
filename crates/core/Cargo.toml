[package]
name = "sightline-core"
description = "Reading, steering and starting Claude Code sessions"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
chrono.workspace = true
crossterm.workspace = true
portable-pty.workspace = true
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
vt100.workspace = true

[target.'cfg(unix)'.dependencies]
# Only to put the daemon in a session of its own, so closing the terminal that
# started it does not take the sessions with it.
libc.workspace = true
